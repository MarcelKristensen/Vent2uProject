import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Vent2uProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
